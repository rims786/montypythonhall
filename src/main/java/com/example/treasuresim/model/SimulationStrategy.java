package com.example.treasuresim.model;

/**
 * Enum representing the player's strategy in the Monty Hall problem:
 * SWITCH - switch the initial door choice after the host opens a door.
 * STAY   - keep the initial door choice.
 */
public enum SimulationStrategy {
    SWITCH,
    STAY
}
