package com.example.treasuresim;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Integration test for the Treasure Simulator application.
 * Ensures that the Spring application context loads correctly.
 */
@SpringBootTest
class TreasureSimulatorApplicationTests {

	/**
	 * Tests if the Spring application context loads without any issues.
	 * This verifies the basic configuration and setup of the application.
	 */
	@Test
	void contextLoads() {
		// Verifies that the Spring application context loads successfully
	}

}
